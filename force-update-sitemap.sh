#!/bin/bash
# Force sitemap update by touching deployment trigger

# Add a comment to force rebuild
# This will make Vercel rebuild and regenerate the sitemap with latest www URLs

echo "Ready to deploy latest sitemap with www URLs"

