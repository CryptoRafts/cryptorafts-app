rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for development/testing
    // This ensures VC dashboard works without permission issues
    allow read, write: if true;
  }
}
