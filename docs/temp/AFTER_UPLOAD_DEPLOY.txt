cd /var/www/cryptorafts && echo "📦 Extracting package..." && unzip -o cryptorafts-deploy.zip && rm -f cryptorafts-deploy.zip && echo "✅ Extracted" && echo "" && echo "📋 Verifying files..." && FILE_COUNT=$(find src -type f | wc -l) && echo "Files in src/: $FILE_COUNT" && if [ $FILE_COUNT -lt 100 ]; then echo "❌ ERROR: Only $FILE_COUNT files found - need to upload cryptorafts-deploy.zip!"; echo "Upload location: C:\Users\dell\AppData\Local\Temp\cryptorafts-deploy.zip"; exit 1; fi && echo "✅ Real files found!" && echo "" && echo "🚀 Running deployment..." && bash DEPLOY_FRESH_COMPLETE.sh

