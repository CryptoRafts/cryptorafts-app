❌ FILES NOT FOUND - UPLOAD VIA HOSTINGER FILE MANAGER
=====================================================

No app files found on VPS!
You need to upload them via Hostinger File Manager.


STEP 1: Prepare Files Locally
-------------------------------
1. Open your local DEPLOY_TO_VPS folder on your Windows computer:
   C:\Users\dell\cryptorafts-starter\DEPLOY_TO_VPS

2. You should see:
   - src/ folder
   - package.json
   - next.config.js
   - tsconfig.json
   - public/ folder (if exists)
   - All other files


STEP 2: Upload via Hostinger File Manager
------------------------------------------
1. Go to: https://hpanel.hostinger.com/
2. Login with your Hostinger account
3. Click on: "File Manager" (or "Files")
4. IMPORTANT: Navigate to /var/www/cryptorafts
   
   Option A: Using Path Bar
   - Look for the PATH BAR at the top of File Manager
   - Click on it or type directly
   - Type: /var/www/cryptorafts
   - Press Enter
   
   Option B: Using Navigation
   - Click on "var" folder
   - Click on "www" folder
   - Click on "cryptorafts" folder (create it if it doesn't exist)
   
5. You should now be in /var/www/cryptorafts

6. Click "Upload" button (usually at top of File Manager)

7. Select ALL files from your local DEPLOY_TO_VPS folder:
   - Select src/ folder (entire folder with all contents)
   - Select package.json
   - Select next.config.js
   - Select tsconfig.json
   - Select public/ folder (if exists)
   - Select .env.production (if needed)
   - Select ALL other files
   
8. Click "Upload" or "Open" to start upload

9. Wait for ALL files to finish uploading
   - Watch for upload progress
   - Make sure ALL files complete successfully


STEP 3: Verify Upload in SSH Terminal
--------------------------------------
After uploading, in your SSH terminal, run:

cd /var/www/cryptorafts
ls -la

You should see:
- package.json
- next.config.js
- tsconfig.json
- src/ directory
- public/ directory (if uploaded)

Then verify:
ls -la package.json
ls -la src/app/page.tsx
ls -la next.config.js

All should exist now!


STEP 4: Install Dependencies and Build
---------------------------------------
cd /var/www/cryptorafts
ls -la package.json

# If file exists, continue:
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use 20
node --version

npm install --legacy-peer-deps
rm -rf .next out node_modules/.cache
NODE_ENV=production npm run build

Wait 5-10 minutes for build to complete!


STEP 5: Create server.js
-------------------------
After build completes, create server.js:

cat > server.js << 'EOF'
const { createServer } = require('http');
const { parse } = require('url');
const next = require('next');

const dev = process.env.NODE_ENV !== 'production';
const hostname = process.env.HOSTNAME || 'localhost';
const port = parseInt(process.env.PORT || '3000', 10);

const app = next({ dev, hostname, port });
const handle = app.getRequestHandler();

app.prepare().then(() => {
  createServer(async (req, res) => {
    try {
      const parsedUrl = parse(req.url, true);
      await handle(req, res, parsedUrl);
    } catch (err) {
      console.error('Error occurred handling', req.url, err);
      res.statusCode = 500;
      res.end('internal server error');
    }
  })
    .once('error', (err) => {
      console.error(err);
      process.exit(1);
    })
    .listen(port, () => {
      console.log(`> Ready on http://${hostname}:${port}`);
    });
});
EOF


STEP 6: Create ecosystem.config.js and Start PM2
--------------------------------------------------
pm2 stop all
pm2 delete all

NODE_PATH=$(which node)
cat > ecosystem.config.js << EOF
module.exports = {
  apps: [
    {
      name: 'cryptorafts',
      script: './server.js',
      instances: 1,
      exec_mode: 'fork',
      interpreter: '$NODE_PATH',
      env: {
        NODE_ENV: 'production',
        PORT: 3000,
      },
      error_file: './logs/pm2-error.log',
      out_file: './logs/pm2-out.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
      merge_logs: true,
      autorestart: true,
      watch: false,
      max_memory_restart: '1G',
      min_uptime: '10s',
      max_restarts: 10,
      restart_delay: 4000,
    },
  ],
};
EOF

mkdir -p logs
pm2 start ecosystem.config.js
pm2 save
pm2 status


STEP 7: Verify
--------------
curl http://localhost:3000

Should show HTML (not error)!


THEN VISIT: https://www.cryptorafts.com


CRITICAL NOTES:
===============
1. Upload to /var/www/cryptorafts (NOT public_html)
2. Upload ALL files including src/ folder
3. Wait for upload to complete before running commands
4. Verify files exist with ls -la before proceeding

