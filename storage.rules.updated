rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow all read and write operations for development
      allow read, write: if true;
    }
  }
}
