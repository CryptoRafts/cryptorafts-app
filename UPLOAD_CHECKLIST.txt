═══════════════════════════════════════════════════════
📋 FILE UPLOAD CHECKLIST - FOLLOW THESE STEPS
═══════════════════════════════════════════════════════

❌ ERROR: Files are NOT uploaded to /var/www/cryptorafts
❌ ERROR: Cannot find module 'next'

The deployment failed because files were NOT uploaded first!


═══════════════════════════════════════════════════════
📋 STEP 1: GO TO HOSTINGER FILE MANAGER
═══════════════════════════════════════════════════════

1. Open your web browser
2. Go to: https://hpanel.hostinger.com/
3. Log in with your credentials
4. Click "File Manager" (or "Files")


═══════════════════════════════════════════════════════
📋 STEP 2: NAVIGATE TO /var/www/cryptorafts
═══════════════════════════════════════════════════════

1. Look for the PATH BAR at the top of the File Manager
2. Click in the PATH BAR
3. Type: /var/www/cryptorafts
4. Press Enter or click "Go"

⚠️  IMPORTANT: Make sure you're in /var/www/cryptorafts
   NOT in public_html or any other directory!


═══════════════════════════════════════════════════════
📋 STEP 3: UPLOAD FILES FROM DEPLOY_TO_VPS FOLDER
═══════════════════════════════════════════════════════

On your LOCAL computer, navigate to:
C:\Users\dell\cryptorafts-starter\DEPLOY_TO_VPS

Upload ALL of these files/folders to /var/www/cryptorafts:

✅ REQUIRED FILES (MUST UPLOAD):
   □ src/ folder
      → This is the ENTIRE src/ folder
      → Drag and drop the entire src/ folder
      → Wait for upload to complete
   
   □ package.json
      → Upload this file
   
   □ next.config.js
      → Upload this file
   
   □ tsconfig.json
      → Upload this file

✅ OPTIONAL FILES:
   □ public/ folder (if it exists)
      → Upload the entire public/ folder

⚠️  DO NOT UPLOAD:
   ❌ node_modules/ (will be created by npm install)
   ❌ .next/ (will be created by npm run build)
   ❌ .git/ (not needed for deployment)


═══════════════════════════════════════════════════════
📋 STEP 4: VERIFY FILES UPLOADED
═══════════════════════════════════════════════════════

After uploading, verify files are in /var/www/cryptorafts:

In Hostinger File Manager:
   □ Check that package.json exists
   □ Check that src/ folder exists
   □ Check that src/app/page.tsx exists
   □ Check that next.config.js exists


═══════════════════════════════════════════════════════
📋 STEP 5: RUN CHECK SCRIPT IN SSH
═══════════════════════════════════════════════════════

After uploading files, run this in your SSH terminal:

cd /var/www/cryptorafts && bash CHECK_FILES_AND_FIX.sh

OR run this to check files manually:

cd /var/www/cryptorafts
ls -la package.json
ls -la src/app/page.tsx
ls -la next.config.js

If all files exist, continue to Step 6.
If any files are missing, go back to Step 3 and upload them.


═══════════════════════════════════════════════════════
📋 STEP 6: RUN DEPLOYMENT
═══════════════════════════════════════════════════════

After verifying files are uploaded, run deployment:

cd /var/www/cryptorafts && bash CHECK_FILES_AND_FIX.sh

This script will:
   1. Check if files exist
   2. Install Node.js 20
   3. Install dependencies
   4. Build the app
   5. Start PM2
   6. Verify deployment


═══════════════════════════════════════════════════════
📋 TROUBLESHOOTING
═══════════════════════════════════════════════════════

If you get "package.json not found":
   → Files were NOT uploaded correctly
   → Go back to Step 3 and upload files again

If you get "Cannot find module 'next'":
   → Files were uploaded but npm install didn't run
   → Make sure package.json exists first
   → Then run: npm install --legacy-peer-deps

If files are in wrong directory:
   → Make sure you're in /var/www/cryptorafts
   → NOT in public_html or home directory
   → Check PATH BAR shows: /var/www/cryptorafts


═══════════════════════════════════════════════════════
📋 QUICK COMMAND REFERENCE
═══════════════════════════════════════════════════════

Check files:
cd /var/www/cryptorafts && ls -la

Check specific file:
ls -la package.json
ls -la src/app/page.tsx

Check all files in directory:
ls -la

Run deployment check:
cd /var/www/cryptorafts && bash CHECK_FILES_AND_FIX.sh


═══════════════════════════════════════════════════════
🎯 SUMMARY - DO THIS NOW
═══════════════════════════════════════════════════════

1. ✅ Go to https://hpanel.hostinger.com/
2. ✅ Click "File Manager"
3. ✅ Navigate to /var/www/cryptorafts (type in PATH BAR)
4. ✅ Upload ALL files from DEPLOY_TO_VPS folder:
   - src/ folder (entire folder)
   - package.json
   - next.config.js
   - tsconfig.json
   - public/ folder (if exists)
5. ✅ Wait for upload to complete
6. ✅ Verify files uploaded (check File Manager)
7. ✅ Run in SSH: cd /var/www/cryptorafts && bash CHECK_FILES_AND_FIX.sh


═══════════════════════════════════════════════════════

